apiversion: v1
kind: DaemonSet
metadata:
 name: fluentd-ds
 labels:
  name: fluentd-ds
spec:
 containers:
  name: fluentd-ds
  image：dockeryanxiao/fluentd-docker
  volumeMounts:
  - name: messagelog
    path: /var/log/message
  - name: varlog
    path: /var/log
  resources:
   limit:
    cpu: 500m
    memory: 500Mi
   request:
    cpu:256m
    memory: 256Mi
 volume:
  - name: messagelog
    hostPath:
     path: /var/log/message
     readOnly: true
  - name: varlog
    hostPath:
     path: /var/log
  
